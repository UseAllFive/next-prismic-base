import { api } from 'lib/api'

export const homeID = api.HOME_ID

// TODO continue to add in fetch links as needed for slices and page content here
export const pageFetchLinks = ['page.page_slug', 'page.parent_page']
